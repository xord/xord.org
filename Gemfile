source "https://rubygems.org"

# Use the github-pages gem to stay in sync with what GitHub Pages actually runs.
gem "github-pages", group: :jekyll_plugins

# Plugins used by the site.
group :jekyll_plugins do
  gem "jekyll-seo-tag"
  gem "jekyll-sitemap"
end
